# M2 Installation

Copy the `docs`, `config`, and `tests` folders into the root of the local project.

Allow Windows to merge folders.

Then run:

```powershell
python -m pytest
python -m ruff check .
```

Expected result:

```text
6 passed
All checks passed!
```
